// Variable declarations with literals
szám fibo0 = 0;
szám fibo1 = 1;
valós pi = 3.1415;
betü letter = 'x';
vajon flag = igaz;

// Reassignments using arithmetic and unary minus
fibo0 = fibo0 + 5;
fibo1 = -fibo1;
pi = pi - 1.0;
letter = 'y';
flag = hamis;

// IO with variables and literals
kiír fibo0;
kiír pi;
kiír 'z';
kiír igaz;
beolvas fibo1;
beolvas pi;

// Arithmetic expressions with nested parentheses
szám result = ((fibo0 + fibo1) * (fibo0 - fibo1)) / 2;

// Logical expressions with all operators
vajon check = (fibo0 >= fibo1) és (flag vagy nem flag);
vajon neg = nem (fibo0 < fibo1 vagy hamis);
vajon complex = (fibo0 nem egyenlő fibo1) és (nem igaz vagy hamis);

// Conditionals with single and multi-statement blocks
ha (fibo0 egyenlő 5) akkor {
    kiír fibo0;
} különben {
    kiír fibo1;
}

ha (flag és nem igaz) akkor {
    kiír letter;
}

// Loops with multiple statements
szám count = 3;
amíg (count > 0) {
    kiír count;
    count = count - 1;
    kiír count;
}

// Nested expressions with all numeric types
valós complexCalc = ((pi + 2.0) * (fibo0 - 3)) / -2.5;

// Edge cases: empty block and unary minus
ha (fibo0 egyenlő 0) akkor {

    amíg (alma >= korte) { }
 }
szám neg = -10;

// Using literal constants directly
kiír 42;
kiír 3.14;
kiír 'a';
kiír hamis;
kiír igaz;